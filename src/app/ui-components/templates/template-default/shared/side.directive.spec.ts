import { SideDirective } from './side.directive';

describe('SideDirective', () => {
  it('should create an instance', () => {
    const directive = new SideDirective();
    expect(directive).toBeTruthy();
  });
});
